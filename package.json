{
  "name": "gastos-familiares",
  "version": "1.0.0",
  "description": "App de gastos familiares (demo para webinar) - NestJS + frontend",
  "private": true,
  "scripts": {
    "install": "npm --prefix backend install && npm --prefix frontend install",
    "postinstall": "echo \"\\n✓ Listo. Ejecutá: npm start\\n\"",
    "start": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run start\" \"npm --prefix frontend run start\"",
    "start:backend": "npm --prefix backend run start",
    "start:frontend": "npm --prefix frontend run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
